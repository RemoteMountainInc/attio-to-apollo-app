import type { ObjectSlug } from "../object-slug";
import type { FormValue } from "./value";
export interface FormAttioRecord<TOptional extends boolean = false, TObjectSlug extends ObjectSlug = ObjectSlug> extends FormValue {
    type: "attio-record";
    /**
     * Whether this Attio record is optional, i.e. allows `undefined` values.
     */
    isOptional: TOptional;
    /**
     * Make this Attio record optional, i.e. allow `undefined` values.
     */
    optional(): FormAttioRecord<true, TObjectSlug>;
    /**
     * Set the object slug(s) of this Attio record.
     */
    objectSlug<T extends ObjectSlug | Array<ObjectSlug>>(value: T): FormAttioRecord<TOptional, T extends Array<infer U> ? U : T>;
}
/**
 * Create a new Attio record form value.
 */
export declare function attioRecord(): FormAttioRecord<false, ObjectSlug>;
//# sourceMappingURL=attio-record.d.ts.map